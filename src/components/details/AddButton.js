import './AddButton.css'

function AddButton() {
    return (
        <button
            className="add-btn">
            <i className="fas fa-shopping-cart"></i>
            add to cart
        </button>
    );
}

export default AddButton;